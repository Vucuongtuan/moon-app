import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  ctn: {
    paddingVertical: 48,
  },
  titleTxt: {
    textAlign: "center",
    fontSize: 60,
  },
  descTxt: {
    textAlign: "left",
    fontSize: 24,
    color: "#6b7280",
    marginTop: 16,
  },
});
